/**
 * 
 * Enum constant
 * @author - NA 
 * @date - 3th September, 2022
 * 
 */
// NOTIFICATION TYPE
export enum NotificationType {
    success = 'success',
    error = 'error',
}

// STATUS TYPE
export enum StatusType {
    completed = 1,
    pending = 2,
    rejected = 3,
}